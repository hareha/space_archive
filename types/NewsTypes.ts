/**
 * 뉴스 관련 타입 정의 (Supabase 스키마 기반)
 * 
 * @deprecated NewsService.ts에서 타입을 직접 export합니다.
 * 기존 import 호환을 위해 re-export 유지.
 */
export type { NewsArticle, SourceType } from '@/services/NewsService';
